# Pack de arranque — agente de código

Copia a la **raíz del repo de código** estos tres elementos:

| Qué | Para qué |
|-----|----------|
| `memory.md` | Toda la info del proyecto (única fuente de verdad) |
| `AGENTS.md` | Resumen mínimo que el agente ve al abrir el repo |
| `.cursor/rules/agent.mdc` | Cómo debe trabajar (siempre activo) |

No hace falta copiar este `README.md`.

Opcional: workspace multi-root con `agente_clon` + el repo de código.
